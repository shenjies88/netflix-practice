server:
  port: 7001
spring:
  application:
    name: eureka-config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/shenjies88/spring-cloud-config-repo
          searchPaths: config-repo
          username: shenjies88
          password: ab1234567890
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:1111/eureka
